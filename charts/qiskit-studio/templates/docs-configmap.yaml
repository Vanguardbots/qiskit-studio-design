apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "qiskit-studio.docsConfigMapName" . }}
  labels:
    {{- include "qiskit-studio.labels" . | nindent 4 }}
data:
  {{- range $path, $bytes := .Files.Glob "maestro/documentation/*" }}
  {{- $file := base $path }}
  {{ $file | quote }}: |-
    {{ $.Files.Get $path | toJson | nindent 4 }}
  {{- end }}